import Link from "next/link";import {notFound} from "next/navigation";import {stories,industryName} from "@/data/content";
export default async function Story({params}:{params:Promise<{id:string}>}){const{id}=await params;const s=stories.find(x=>x.id===id);if(!s)return notFound();return <main className="reading"><Link className="back" href="/stories">← 返回薪火文集</Link><p className="eyebrow">{industryName(s.industry)} · {s.year}</p><h1>{s.title}</h1><div className="byline"><b>{s.name}</b><span>{s.background}</span></div><hr/><article><p>{s.excerpt}</p><h2>从模糊的兴趣开始</h2><p>回头看，求职最有价值的部分不是某一个结果，而是逐渐找到自己可以长期投入的问题。我一开始也把信息收集等同于准备，后来才意识到，需要的是对选择的理解和自己的表达。</p><h2>把每次尝试当成一次积累</h2><p>我会记录每次交流、申请和练习中得到的反馈。它们让我更清楚自己的优势，也帮助我在后续对话中给出更真实、更具体的回答。</p></article><Link className="related-link" href={`/industries/${s.industry}`}>→ 探索{industryName(s.industry)}行业</Link></main>}
