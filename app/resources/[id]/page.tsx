import Link from "next/link";import {notFound} from "next/navigation";import {resources,industryName} from "@/data/content";
export default async function Resource({params}:{params:Promise<{id:string}>}){const{id}=await params;const r=resources.find(x=>x.id===id);if(!r)return notFound();return <main className="reading"><Link className="back" href="/companies">← 返回知识库</Link><p className="eyebrow">{r.kind} · {r.date}</p><h1>{r.title}</h1><p className="dek">{r.summary}</p><hr/><article><p>这是一篇为事业部智库原型撰写的示例内容。好的准备，不是收集更多碎片化的信息，而是建立自己的判断框架：了解行业如何运作、岗位真正需要什么，以及你有哪些可信的经历可以支持这份选择。</p><h2>从问题开始，而不是从模板开始</h2><p>先问自己：我在尝试理解什么？是一个行业的工作方式、一家公司正在解决的问题，还是自己经验中可被验证的能力？当这些问题足够清楚，接下来的资料检索、经历梳理和表达都更有方向。</p><p>把每一次准备视为一次小型研究：查找可靠信息，比较不同观点，并留下自己的判断。这样形成的材料和回答会更具体，也更有辨识度。</p></article><Link className="related-link" href={r.industry!=="all"?`/industries/${r.industry}`:"/companies"}>→ 探索{r.industry!=="all"?industryName(r.industry):"相关公司"}</Link></main>}
