import {events} from "@/data/content";export default function Events(){return <main className="page"><p className="eyebrow">WHAT'S NEXT</p><h1>活动日历</h1><p className="intro">面向求职准备的分享、工作坊与实践活动。所有活动均通过外部链接报名。</p><div className="calendar">{events.map(e=><article className="event-row" key={e.id}><time>{e.date}</time><div><h2>{e.title}</h2><p>{e.time} · {e.location}</p><p>{e.description}</p><small>主办：{e.organiser}</small></div><a href={e.signupUrl} target="_blank">报名 →</a></article>)}</div></main>}
